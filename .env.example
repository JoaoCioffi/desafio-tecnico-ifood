# Copie para .env e preencha a chave. O .env nao e versionado.
#
# `openai-api` e o nome que o Hermes usa para o endpoint da OpenAI — ele nao
# conhece "openai", e o erro so aparece na primeira chamada.
LLM_PROVIDER=openai-api
LLM_PROVIDER_API_KEY=
LLM_MODEL=gpt-5.6-terra
